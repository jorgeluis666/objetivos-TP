{
  "name": "objetivos-terminal-pesquero",
  "version": "1.10.0",
  "private": true,
  "description": "Dashboard de objetivos e inversion publicitaria de Terminal Pesquero",
  "scripts": {
    "dev": "live-server --port=3000 --open=index.html --no-browser",
    "build": "node scripts/build.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {},
  "devDependencies": { "live-server": "^1.2.2" }
}
